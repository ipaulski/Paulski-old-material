import javax.swing.*;
public class GreetingRandom_Paulski
{
    public GreetingRandom_Paulski(){}
    
    public void given(){
        String last = JOptionPane.showInputDialog(f, "Enter last name");
        String first = JOptionPane.showInputDialog(f, "Enter first name");
        String a = "Hello, "+first+" "+last;
        String b = "Good morning "+first +" "+last;
        String c = "Good bye "+first +" "+last;
        
        JOptionPane.showMessageDialog(f, );
    }
}
